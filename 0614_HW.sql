SELECT
    MGR
FROM EMP
WHERE ENAME = 'SCOTT';

SELECT
    MGR
FROM EMP
WHERE ename='KING';

SELECT
    NVL(MGR,9999) AS MGR
FROM emp
WHERE ename='KING';

SELECT
    MGR
FROM EMP
WHERE ENAME = 'JSC';


SELECT
    NVL(MGR,9999) AS MGR
FROM emp
WHERE ename = 'JSC';